pub fn encode(image_data: Vec<[u8; 4]>, image_dimensions: [u8; 2]) -> Vec<u8> { // converts an array of RGBA pixels into the qoi format
    //TODO
    let previous: Vec<[u8; 4]> = Vec::new();
    for i in image_data {

    }
}